# Trilogue 環境変数テンプレート
# 各自このファイルを .env.local にコピーし、自分のキーを設定すること。
#   cp .env.local.example .env.local
# .env.local は .gitignore 済み。共有物（リポジトリ等）に実キーを絶対に入れないこと。

# OpenAI APIキー（必須）。OpenAIダッシュボードで発行した値を各自設定する。
OPENAI_API_KEY=

# 使用モデル（任意）。未設定時は gpt-4o-mini が使われる。
OPENAI_MODEL=gpt-4o-mini
